# tests

NixOS VM tests for the gateway and the runner go here once those exist
(handoff item 31). Until then the Haskell test suite under haskell/test is
the only automated test, and the mock servers used during development are
throwaway Python in a sandbox, not part of this repo.
